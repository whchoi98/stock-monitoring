# 진양성 픽스처 — 주석·빈 줄을 제외한 모든 줄이 secret-scan.sh 패턴에 최소 1개 매치해야 함.
# True-positive fixtures — every non-comment line MUST match at least one secret-scan.sh pattern.
#
# 주의: 실제 토큰 형식(Slack/GitHub/OpenAI/Anthropic/Stripe/Google/AWS Secret Key)은
# GitHub Push Protection을 피하기 위해 이 파일에 두지 않는다 —
# tests/hooks/test-secret-patterns.sh 에서 런타임 문자열 연결로 생성해 검증한다.
# Note: real provider token shapes (Slack/GitHub/OpenAI/Anthropic/Stripe/Google/AWS secret key)
# are NOT stored here to avoid GitHub Push Protection — they are constructed at runtime
# via string concatenation in tests/hooks/test-secret-patterns.sh.

# AWS Access Key ID (AWS 공식 문서의 예제 키 / canonical example key from AWS documentation)
AKIAIOSFODNN7EXAMPLE

# 일반 자격증명 대입문 / Generic credential assignments
password = "hunter2hunter2"
db_secret: "correct-horse-battery"
api_key = "0123456789abcdef0123"
x-api-key: "abcdefgh12345678"

# Azure Storage 연결 문자열 접두부 (키 없음) / Azure connection string prefix (no key material)
DefaultEndpointsProtocol=https;AccountName=examplestorage
