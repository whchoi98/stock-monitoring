/**
 * 앱 셸 자리표시자 — F4에서 QueryClientProvider + RouterProvider + 네비/TickerBar로 교체된다.
 * App shell placeholder; replaced in F4 with providers, nav and TickerBar.
 */
export default function App() {
  return null
}
