# Backend (FastAPI) Implementation Plan — Plan 1/3

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** stock-on-tui의 데이터/AI 로직을 yfinance 단일 소스 + L1/L2 계층 캐시 구조의 FastAPI 백엔드로 포팅한다.

**Architecture:** FastAPI 단일 앱. `services/`가 yfinance/RSS/Bedrock을 호출하고, `cache/tiered.py`가 L1(인메모리)→L2(DynamoDB)→fetch 순서를 오케스트레이션. lifespan에서 백그라운드 갱신 루프(시세 45s/뉴스 120s, 휴장 시 600s) 기동.

**Tech Stack:** Python 3.12, FastAPI, uvicorn, yfinance, httpx, boto3, pydantic v2, pytest, pytest-asyncio, moto

**Spec:** `docs/superpowers/specs/2026-08-01-stock-monitoring-design.md` (승인본 — 항상 우선)
**포팅 원본:** `/home/ec2-user/my-project/stock-on-tui` (이하 `$TUI`)

## Global Constraints

- symbol 형식: yfinance 티커 통일 — US `AAPL`, KR `005930.KS` / `247540.KQ`
- 모든 시세 응답 envelope: `{"asOf": "<ISO8601>", "marketOpen": bool, "data": ...}`
- 시뮬레이션 응답(호가/수급)에 `"simulated": true` 필수
- 조용한 실패 금지: 실패는 structlog 스타일 JSON 로그 + `/api/health`의 소스 상태에 반영
- DynamoDB 테이블 env `CACHE_TABLE` (기본 `stock-monitoring-cache`), 항목: `pk`(S, 키), `data`(S, JSON), `ttl`(N, epoch), `asOf`(S)
- Bedrock 모델: env `BEDROCK_MODEL_ID` 기본 `us.anthropic.claude-sonnet-4-6`, 리전 `BEDROCK_REGION` 기본 `ap-northeast-2`
- 캐시 TTL(초): quotes/overview/news `86400`(L2 보존용), chart `{"1w":600,"1m":3600,"3m":21600,"1y":86400}`, fundamentals `43200`, ai `21600`
- 가격 오버레이(사용자 결정 2026-08-02): `detail:{symbol}`은 느린 재무지표(PER/EPS/beta/52주 등)만 `FUNDAMENTALS_TTL`로 캐시하고, 응답 시점에 `quotes:{market}` 캐시의 실시간 `price/change/change_pct/volume`으로 덮어써 반환한다. 호가/수급 시드도 이 실시간 가격 기준(`build_order_book(live_price, market, seed=int(live_price*100))`). quotes 캐시에 해당 심볼이 없으면 캐시된 detail 값으로 폴백.
- AI rate limit: IP당 분당 3회 + 전역 동시 2건(asyncio.Semaphore), 초과 429
- 테스트에서 실제 네트워크/AWS 호출 금지 (yfinance·httpx·boto3 모두 mock/moto)
- 커밋 메시지: conventional commits (`feat:`, `test:`, `chore:`)

---

### Task B1: 프로젝트 스캐폴드 + config + models

**Files:**
- Create: `backend/requirements.txt`, `backend/requirements-dev.txt`, `backend/pytest.ini`
- Create: `backend/app/__init__.py`, `backend/app/core/__init__.py`, `backend/app/core/config.py`, `backend/app/models.py`
- Test: `backend/tests/test_config.py`

**Interfaces (Produces):**
- `app.core.config`: `REFRESH_INTERVAL=45`, `NEWS_REFRESH_INTERVAL=120`, `CLOSED_REFRESH_INTERVAL=600`, `US_INDICES`, `KR_INDICES`, `US_STOCKS: list[str]`(yfinance 티커), `KR_STOCKS: list[str]`(`.KS`/`.KQ` 접미사 포함), `STOCK_NAMES: dict[str,str]`, `STOCK_SECTORS: dict[str,str]`, `INDICATORS`, `SECTOR_INDICATOR_MAP`, `NEWS_FEEDS`, `CHART_TTL: dict[str,int]`, `FUNDAMENTALS_TTL=43200`, `AI_TTL=21600`, `L2_TTL=86400`, `CACHE_TABLE`, `BEDROCK_MODEL_ID`, `BEDROCK_REGION`, `AI_RATE_PER_MIN=3`, `AI_GLOBAL_CONCURRENCY=2`
- `app.models` (pydantic v2): `Quote(symbol,name,price,change,change_pct,volume,market,currency,sector,market_cap|None)`, `IndexQuote(symbol,name,value,change,change_pct)`, `Indicator(symbol,name,value,change,change_pct,unit)`, `NewsItem(id,title,link,source,published,language)`, `Candle(time,open,high,low,close,volume)`, `ChartResponse(symbol,period,candles,ma5:list[float|None],ma20:list[float|None],signals:list[CrossSignal])`, `CrossSignal(time,kind:Literal["golden","dead"])`, `StockDetailResponse`(TUI `$TUI/models/stock.py` `StockDetail` 필드에서 history_* 제외 + `pbr|None` 추가), `OrderBookEntry(price,qty,side:Literal["bid","ask"])`, `InvestorRow(date,individual,foreign,institution)`, `Envelope(asOf:str,marketOpen:bool,data:Any)` 제네릭 아님, dict 반환 헬퍼 `envelope(data, market_open)` 사용

- [ ] **Step 1: 디렉토리/의존성 파일 작성**

`backend/requirements.txt`:
```
fastapi>=0.115
uvicorn[standard]>=0.30
yfinance>=0.2.40
httpx>=0.27
boto3>=1.34
pydantic>=2.7
```
`backend/requirements-dev.txt`:
```
-r requirements.txt
pytest>=8
pytest-asyncio>=0.23
moto[dynamodb]>=5
```
`backend/pytest.ini`:
```ini
[pytest]
asyncio_mode = auto
testpaths = tests
```

- [ ] **Step 2: config.py 작성** — `$TUI/config.py`의 `US_STOCKS`/`US_STOCK_NAMES`/`US_STOCK_SECTORS`/`KR_STOCKS`/`KR_STOCK_NAMES`/`KR_STOCK_SECTORS`/`US_INDICES`/`KR_INDICES`/`INDICATORS`/`SECTOR_INDICATOR_MAP`/`NEWS_FEEDS`를 **그대로 복사**하되 다음 변환 적용:
  - `KR_STOCKS`의 6자리 코드에 접미사 부여: KOSDAQ 종목 `247540,036570(주의: 036570 NCsoft는 KOSPI)` → 정확 규칙: KOSDAQ = `["247540","259960","326030","323410","361610","352820","066970"]` 중 목록에 있는 것(`247540`,`259960`,`326030`,`323410`,`361610`,`352820`)만 `.KQ`, 나머지는 `.KS`
  - `KR_STOCK_NAMES`/`KR_STOCK_SECTORS` 키도 동일 접미사 키로 재생성 (dict comprehension)
  - `STOCK_NAMES = {**US_STOCK_NAMES, **KR_STOCK_NAMES}`, `STOCK_SECTORS` 동일
  - Global Constraints의 TTL/env 상수 추가 (`os.environ.get` 사용)

- [ ] **Step 3: models.py 작성** — Interfaces 정의 그대로. `envelope()` 헬퍼:
```python
from datetime import datetime, timezone

def envelope(data, market_open: bool, as_of: str | None = None) -> dict:
    return {"asOf": as_of or datetime.now(timezone.utc).isoformat(), "marketOpen": market_open, "data": data}
```

- [ ] **Step 4: 검증 테스트 작성/실행** — `backend/tests/test_config.py`:
```python
from app.core import config

def test_kr_symbols_have_suffix():
    assert all(s.endswith((".KS", ".KQ")) for s in config.KR_STOCKS)
    assert "005930.KS" in config.KR_STOCKS and "247540.KQ" in config.KR_STOCKS
    assert len(config.KR_STOCKS) == 50 and len(config.US_STOCKS) == 50

def test_names_and_sectors_cover_universe():
    for s in config.US_STOCKS + config.KR_STOCKS:
        assert s in config.STOCK_NAMES and s in config.STOCK_SECTORS
```
Run: `cd backend && python -m venv .venv && .venv/bin/pip install -r requirements-dev.txt && .venv/bin/pytest tests/test_config.py -v` → PASS

- [ ] **Step 5: Commit** — `git add backend && git commit -m "feat(backend): scaffold, config universe, pydantic models"`

---

### Task B2: market_hours — 장 시간 판정 (TDD)

**Files:**
- Create: `backend/app/core/market_hours.py`
- Test: `backend/tests/test_market_hours.py`

**Interfaces (Produces):**
- `is_kr_market_open(now: datetime) -> bool` — KST 평일 09:00~15:30
- `is_us_market_open(now: datetime) -> bool` — America/New_York 평일 09:30~16:00 (zoneinfo가 DST 처리)
- `any_market_open(now) -> bool`, `refresh_interval(now) -> int` — 열려 있으면 45, 아니면 600

- [ ] **Step 1: 실패 테스트 작성**
```python
from datetime import datetime
from zoneinfo import ZoneInfo
from app.core.market_hours import is_kr_market_open, is_us_market_open, refresh_interval

KST = ZoneInfo("Asia/Seoul")

def test_kr_open_weekday_10am():
    assert is_kr_market_open(datetime(2026, 8, 3, 10, 0, tzinfo=KST))  # 월요일

def test_kr_closed_weekend_and_evening():
    assert not is_kr_market_open(datetime(2026, 8, 1, 10, 0, tzinfo=KST))  # 토요일
    assert not is_kr_market_open(datetime(2026, 8, 3, 15, 31, tzinfo=KST))

def test_us_open_handles_dst():
    # 2026-08-03 10:00 ET(EDT) = 23:00 KST → 미국 장중
    assert is_us_market_open(datetime(2026, 8, 3, 23, 0, tzinfo=KST))
    # 겨울(EST): 2026-01-05 10:00 ET = 2026-01-06 00:00 KST
    assert is_us_market_open(datetime(2026, 1, 6, 0, 0, tzinfo=KST))

def test_refresh_interval():
    assert refresh_interval(datetime(2026, 8, 3, 10, 0, tzinfo=KST)) == 45
    assert refresh_interval(datetime(2026, 8, 2, 3, 0, tzinfo=KST)) == 600  # 일요일 03:00 KST = 토요일 14:00 EDT, 양 시장 휴장
```

- [ ] **Step 2: 실행 → FAIL 확인** — `pytest tests/test_market_hours.py -v` → ImportError

- [ ] **Step 3: 최소 구현**
```python
from datetime import datetime, time
from zoneinfo import ZoneInfo

KST = ZoneInfo("Asia/Seoul")
NY = ZoneInfo("America/New_York")

def _open_between(now: datetime, tz, start: time, end: time) -> bool:
    local = now.astimezone(tz)
    return local.weekday() < 5 and start <= local.time() <= end

def is_kr_market_open(now: datetime) -> bool:
    return _open_between(now, KST, time(9, 0), time(15, 30))

def is_us_market_open(now: datetime) -> bool:
    return _open_between(now, NY, time(9, 30), time(16, 0))

def any_market_open(now: datetime) -> bool:
    return is_kr_market_open(now) or is_us_market_open(now)

def refresh_interval(now: datetime) -> int:
    return 45 if any_market_open(now) else 600
```

- [ ] **Step 4: 실행 → PASS 확인**
- [ ] **Step 5: Commit** — `git commit -m "feat(backend): market hours detection (KST/NY, DST-aware)"`

---

### Task B3: L1 인메모리 캐시 + Tiered 오케스트레이션 (TDD)

**Files:**
- Create: `backend/app/cache/__init__.py`, `backend/app/cache/memory.py`, `backend/app/cache/tiered.py`
- Test: `backend/tests/test_cache_tiered.py`

**Interfaces (Produces):**
- `memory.MemoryCache`: `.get(key) -> tuple[Any, str] | None`(value, asOf), `.set(key, value, ttl: int, as_of: str)` — 만료는 lazy(get 시 검사)
- `tiered.TieredCache(l1: MemoryCache, l2)`: `async get_or_fetch(key: str, ttl: int, fetcher: Callable[[], Awaitable[Any]]) -> tuple[Any, str, str]` — (value, asOf, source∈{"l1","l2","fetch"}). fetch 성공 시 L1+L2 기록, fetch 실패 시 L2 stale 폴백(만료 무시) 후 없으면 raise. `async put(key, value, ttl)` — 스케줄러 선제 기록용
- L2 프로토콜(덕 타이핑): `async get(key) -> tuple[Any, str] | None`, `async get_stale(key) -> tuple[Any, str] | None`, `async put(key, value, ttl, as_of)`

- [ ] **Step 1: 실패 테스트 작성** — FakeL2(dict 기반) + 호출 카운터 fetcher로 4 시나리오:
```python
import pytest
from app.cache.memory import MemoryCache
from app.cache.tiered import TieredCache

class FakeL2:
    def __init__(self): self.store = {}; self.stale = {}
    async def get(self, key): return self.store.get(key)
    async def get_stale(self, key): return self.stale.get(key) or self.store.get(key)
    async def put(self, key, value, ttl, as_of): self.store[key] = (value, as_of)

@pytest.fixture
def cache():
    return TieredCache(MemoryCache(), FakeL2())

async def test_l1_hit_skips_fetch(cache):
    calls = []
    async def fetcher(): calls.append(1); return "fresh"
    v1, _, s1 = await cache.get_or_fetch("k", 60, fetcher)
    v2, _, s2 = await cache.get_or_fetch("k", 60, fetcher)
    assert (v1, s1) == ("fresh", "fetch") and (v2, s2) == ("fresh", "l1") and len(calls) == 1

async def test_l2_hit_populates_l1(cache):
    await cache.l2.put("k", "from-l2", 60, "2026-08-01T00:00:00Z")
    async def fetcher(): raise AssertionError("must not fetch")
    v, as_of, s = await cache.get_or_fetch("k", 60, fetcher)
    assert (v, s) == ("from-l2", "l2")
    assert cache.l1.get("k") is not None

async def test_fetch_writes_both(cache):
    async def fetcher(): return {"a": 1}
    await cache.get_or_fetch("k", 60, fetcher)
    assert (await cache.l2.get("k"))[0] == {"a": 1}

async def test_fetch_failure_falls_back_to_stale(cache):
    cache.l2.stale["k"] = ("stale-data", "2026-07-31T00:00:00Z")
    async def fetcher(): raise RuntimeError("yahoo down")
    v, as_of, s = await cache.get_or_fetch("k", 60, fetcher)
    assert (v, s) == ("stale-data", "l2-stale")
```

- [ ] **Step 2: 실행 → FAIL 확인**
- [ ] **Step 3: 구현** — `MemoryCache`는 `dict[str, tuple[value, as_of, expires_at]]` + `time.monotonic()`. `TieredCache.get_or_fetch`: L1→L2→fetcher try/except→`l2.get_stale` 순. fetch 성공 시 `as_of = datetime.now(timezone.utc).isoformat()`으로 L1/L2 기록. 실패 폴백 시 source `"l2-stale"` + `logger.warning(json.dumps({...}))`.
- [ ] **Step 4: 실행 → PASS 확인**
- [ ] **Step 5: Commit** — `git commit -m "feat(backend): tiered cache L1->L2->fetch with stale fallback"`

---

### Task B4: L2 DynamoDB 캐시 (moto)

**Files:**
- Create: `backend/app/cache/dynamo.py`
- Test: `backend/tests/test_cache_dynamo.py`

**Interfaces (Produces):** `DynamoCache(table_name: str)` — B3의 L2 프로토콜 구현. `get`은 `ttl < now`면 None(DynamoDB TTL 삭제는 지연되므로 앱에서도 검사), `get_stale`은 ttl 무시. boto3 리소스는 `asyncio.to_thread`로 감쌈. boto3 예외 시 get/get_stale은 None 반환 + warning 로그(L2 장애가 서비스를 죽이면 안 됨).

- [ ] **Step 1: 실패 테스트 작성** — moto `@mock_aws`로 테이블 생성 후: put→get 라운드트립(JSON 직렬화 확인), 만료 항목 get=None·get_stale=값, 테이블 없음 시 get=None(예외 미전파). 테스트 픽스처:
```python
import boto3, pytest, time
from moto import mock_aws
from app.cache.dynamo import DynamoCache

@pytest.fixture
def table():
    with mock_aws():
        boto3.client("dynamodb", region_name="ap-northeast-2").create_table(
            TableName="t", KeySchema=[{"AttributeName": "pk", "KeyType": "HASH"}],
            AttributeDefinitions=[{"AttributeName": "pk", "AttributeType": "S"}],
            BillingMode="PAY_PER_REQUEST")
        yield "t"

async def test_roundtrip_and_expiry(table):
    c = DynamoCache(table)
    await c.put("q", {"x": [1, 2]}, ttl=60, as_of="A")
    assert (await c.get("q")) == ({"x": [1, 2]}, "A")
    await c.put("old", "v", ttl=-10, as_of="B")   # 이미 만료
    assert await c.get("old") is None
    assert (await c.get_stale("old")) == ("v", "B")
```
- [ ] **Step 2: FAIL 확인** → **Step 3: 구현**(`json.dumps`/`loads`, `ttl` epoch = `int(time.time()) + ttl`) → **Step 4: PASS 확인**
- [ ] **Step 5: Commit** — `git commit -m "feat(backend): DynamoDB L2 cache with app-level TTL check"`

---

### Task B5: 시장 데이터 서비스 — 지수/경제지표/시세 (yfinance mock)

**Files:**
- Create: `backend/app/services/__init__.py`, `backend/app/services/market_data.py`
- Test: `backend/tests/test_market_data.py`

**Interfaces (Produces):**
- `fetch_indices() -> list[IndexQuote]` — `$TUI/services/us_stocks.py:fetch_us_indices` + `$TUI/services/kr_stocks.py:fetch_kr_indices` 통합 포팅: `yf.download(list(US_INDICES|KR_INDICES), period="5d")`에서 마지막 2개 종가로 change 계산 (KR도 yfinance `^KS11`/`^KQ11` — pykrx 미사용)
- `fetch_indicators() -> list[Indicator]` — `$TUI/services/indicators.py:fetch_indicators` 로직 포팅 (batch download, dropna, 마지막 2개 종가)
- `fetch_quotes(market: Literal["us","kr"]) -> list[Quote]` — `$TUI/services/us_stocks.py:_parse_df`/`fetch_us_quotes` 청크(10개) 패턴 포팅. KR도 동일 경로(yfinance) 사용, `currency="KRW"`, name/sector는 config 매핑. market_cap은 `yf.download`로는 없으므로 None (B6 fundamentals에서 채움 — 대시보드 테이블은 별도 `fetch_market_caps(symbols)`: `Ticker.fast_info["market_cap"]`를 `ThreadPoolExecutor(10)`로)
- 모든 함수 동기(yfinance가 동기) — 호출부에서 `asyncio.to_thread`

- [ ] **Step 1: 실패 테스트 작성** — `monkeypatch`로 `yfinance.download`를 고정 DataFrame 반환 함수로 대체:
```python
import pandas as pd
from app.services import market_data

def fake_download(symbols, **kw):
    idx = pd.to_datetime(["2026-07-30", "2026-07-31"])
    if isinstance(symbols, str): symbols = [symbols]
    cols = pd.MultiIndex.from_product([symbols, ["Close", "Volume"]])
    df = pd.DataFrame(index=idx, columns=cols, dtype=float)
    for s in symbols:
        df[(s, "Close")] = [100.0, 110.0]
        df[(s, "Volume")] = [1000, 2000]
    return df

def test_fetch_indices_change(monkeypatch):
    monkeypatch.setattr(market_data.yf, "download", fake_download)
    out = market_data.fetch_indices()
    kospi = next(i for i in out if i.symbol == "^KS11")
    assert kospi.value == 110.0 and round(kospi.change_pct, 1) == 10.0

def test_fetch_quotes_kr_currency(monkeypatch):
    monkeypatch.setattr(market_data.yf, "download", fake_download)
    out = market_data.fetch_quotes("kr")
    assert len(out) == 50 and out[0].currency == "KRW" and out[0].symbol.endswith((".KS", ".KQ"))
```
- [ ] **Step 2: FAIL 확인** → **Step 3: 구현**(포팅 — `_safe_float`/`_isnan` 헬퍼 포함, 실패 심볼은 skip+warning) → **Step 4: PASS**
- [ ] **Step 5: Commit** — `git commit -m "feat(backend): indices/indicators/quotes via yfinance only"`

---

### Task B6: 차트 + 펀더멘털 서비스

**Files:**
- Create: `backend/app/services/charts.py`, `backend/app/services/fundamentals.py`
- Test: `backend/tests/test_charts.py`, `backend/tests/test_fundamentals.py`

**Interfaces (Produces):**
- `charts.compute_ma(closes: list[float], window: int) -> list[float | None]` — 앞쪽 window-1개는 None
- `charts.find_crosses(ma5, ma20, times) -> list[CrossSignal]` — 직전 구간 ma5<=ma20 → 현재 ma5>ma20 = golden, 반대 = dead (None 구간 skip)
- `charts.fetch_chart(symbol: str, period: Literal["1w","1m","3m","1y"]) -> ChartResponse` — `yf.Ticker(symbol).history(period=..., interval=...)` 매핑 `{"1w":("7d","1h"),"1m":("1mo","1d"),"3m":("3mo","1d"),"1y":("1y","1d")}` → Candle 리스트 + MA/크로스
- `fundamentals.fetch_detail(symbol: str) -> StockDetailResponse` — `Ticker.fast_info` + `Ticker.info`(trailingPE/trailingEps/beta/priceToBook/dividendYield) + `history("1y")`로 52주고저·기간수익률(1w/1m/3m/1y: 시작가 대비). 결측은 None. KR이면 `pbr=info.get("priceToBook")`

- [ ] **Step 1: MA/크로스 실패 테스트 작성** (순수 로직 — 네트워크 무관):
```python
from app.services.charts import compute_ma, find_crosses

def test_compute_ma():
    assert compute_ma([1, 2, 3, 4], 3) == [None, None, 2.0, 3.0]

def test_golden_and_dead_cross():
    ma5  = [None, 1.0, 2.0, 3.0, 1.0]
    ma20 = [None, 1.5, 1.5, 1.5, 1.5]
    t = ["a", "b", "c", "d", "e"]
    sigs = find_crosses(ma5, ma20, t)
    assert [(s.time, s.kind) for s in sigs] == [("c", "golden"), ("e", "dead")]
```
- [ ] **Step 2: FAIL 확인** → **Step 3: 순수 로직 구현** → **Step 4: PASS**
- [ ] **Step 5: fetch_chart/fetch_detail 구현 + mock 테스트** — `yf.Ticker`를 monkeypatch한 FakeTicker(고정 history DataFrame·fast_info dict·info dict)로: 캔들 개수·MA 길이 일치, 결측 PER→None, 수익률 계산 검증 1건씩.
- [ ] **Step 6: PASS 확인 후 Commit** — `git commit -m "feat(backend): chart MA/cross + fundamentals via yfinance"`

---

### Task B7: 뉴스 서비스 (RSS + 기사 본문 추출 포팅)

**Files:**
- Create: `backend/app/services/news.py`
- Test: `backend/tests/test_news.py`

**Interfaces (Produces):**
- `parse_rss(xml_text: str, source: str, is_korean: bool) -> list[NewsItem]` — `$TUI/services/news.py:_parse_rss` 포팅 (`_clean_html` 포함; EN_KO_TERMS 정적 번역은 **포팅하지 않음** — 웹은 원문 표기)
- `async fetch_news(max_per_source: int = 10) -> list[NewsItem]` — `NEWS_FEEDS` 4종을 `httpx.AsyncClient`로 병렬(gather), 실패 소스는 skip+warning
- `async fetch_company_news(symbol: str) -> list[NewsItem]` — US: `feeds.finance.yahoo.com/rss/2.0/headline?s={base}`, KR: Google News RSS (`$TUI` `fetch_company_news` 포팅, symbol에서 `.KS`/`.KQ` 제거한 코드로 name 조회)
- `async fetch_article_content(url: str) -> str` — `$TUI` `fetch_article_content`의 regex 3단계 전략 그대로 포팅(httpx 비동기화)
- `NewsItem.id = sha1(link)[:16]`, `language ∈ {"ko","en"}`

- [ ] **Step 1: 실패 테스트 작성** — 고정 RSS XML 문자열로 `parse_rss` 항목 수/필드 검증, `<article>` 태그 있는 HTML 문자열로 본문 추출 검증(`httpx.AsyncClient.get`을 respx 없이 monkeypatch), 피드 1개 실패 시 나머지 3개 반환 검증.
- [ ] **Step 2: FAIL 확인** → **Step 3: 포팅 구현** → **Step 4: PASS**
- [ ] **Step 5: Commit** — `git commit -m "feat(backend): RSS news + article extraction (ported)"`

---

### Task B8: 시뮬레이션(호가/수급) + 시장요약 서비스

**Files:**
- Create: `backend/app/services/simulation.py`, `backend/app/services/summary.py`
- Test: `backend/tests/test_simulation.py`, `backend/tests/test_summary.py`

**Interfaces (Produces):**
- `simulation.build_order_book(price: float, market: str, seed: int) -> list[OrderBookEntry]` — `$TUI/services/stock_detail_data.py:fetch_order_book` 포팅하되 현재가를 인자로 받고 `random.Random(seed)` 주입(결정적 테스트). ask 10 + bid 10, KR 틱 단위 규칙 유지
- `simulation.build_investor_trends(history: list[tuple[str, float, int]], market: str) -> list[InvestorRow]` — `_fetch_us_investor_trends`/`_fetch_kr_investor_trends`의 비율 로직(US: 기관70/외인15/개인10, KR: 개인60/외인30/기관10, 개인은 역방향) 포팅, (date, close, volume) 리스트를 인자로
- `summary.build_summary(us: list[Quote], kr: list[Quote]) -> dict` — breadth(상승/하락 수), top_gainers/losers 3, volume_leaders 3, 시장별
- `summary.build_sectors(quotes: list[Quote]) -> list[dict]` — 섹터별 평균 change_pct, |평균| 내림차순 상위 8

- [ ] **Step 1: 실패 테스트 작성** — 고정 Quote 6개로 breadth/top3/섹터 평균·정렬 검증; `build_order_book(70000, "kr", seed=42)` 호출 2회 결과 동일 + ask>현재가>bid + 각 10개; investor 비율 합계 = ±거래량 검증.
- [ ] **Step 2: FAIL 확인** → **Step 3: 포팅/구현** → **Step 4: PASS**
- [ ] **Step 5: Commit** — `git commit -m "feat(backend): simulated orderbook/investors + market summary"`

---

### Task B9: Bedrock AI 서비스

**Files:**
- Create: `backend/app/services/bedrock_ai.py`
- Test: `backend/tests/test_bedrock_ai.py`

**Interfaces (Produces):**
- `$TUI/services/bedrock.py`를 포팅: `is_bedrock_available()`, `analyze_article(title, content, is_korean) -> str`, `analyze_stock(symbol, name, price, change_pct, pe_ratio, week52_high, week52_low, sector, market, news_titles) -> str` — 프롬프트/모델호출 본문 **그대로 유지**, 차이점 3개: ① region 기본 `ap-northeast-2` ② BEDROCK_API_KEY 분기 제거(ECS Task Role 사용) ③ 예외를 문자열 반환 대신 `BedrockUnavailableError`/`BedrockCallError`로 raise (API 계층이 상태코드 매핑)

- [ ] **Step 1: 실패 테스트 작성** — boto3 client를 monkeypatch한 FakeClient(`invoke_model` 고정 응답 `{"content":[{"text":"분석결과"}]}`)로 반환 텍스트 검증 + FakeClient가 받은 modelId/본문 max_tokens 검증 + client 예외 시 `BedrockCallError` raise 검증.
- [ ] **Step 2: FAIL 확인** → **Step 3: 포팅** → **Step 4: PASS**
- [ ] **Step 5: Commit** — `git commit -m "feat(backend): bedrock stock/article analysis (ported, exceptions)"`

---

### Task B10: API 라우트 — health / market / stocks

**Files:**
- Create: `backend/app/api/__init__.py`, `backend/app/api/deps.py`, `backend/app/api/health.py`, `backend/app/api/market.py`, `backend/app/api/stocks.py`
- Create: `backend/app/state.py`
- Test: `backend/tests/test_api_market.py`, `backend/tests/test_api_stocks.py`, `backend/tests/conftest.py`

**Interfaces:**
- Consumes: B3 `TieredCache`, B5~B8 서비스 전부
- Produces: `state.AppState(cache: TieredCache, source_status: dict[str, str])` — FastAPI `app.state.ctx`에 저장, `deps.get_state(request) -> AppState`
- 라우트 (스펙 5.2 그대로): `GET /api/health`(200 고정, body에 `sources`/`cacheAge`), `GET /api/market/overview`, `GET /api/market/quotes?market=us|kr`(잘못된 값 422), `GET /api/market/news`, `GET /api/stocks/{symbol}`, `GET /api/stocks/{symbol}/chart?period=`, `GET /api/stocks/{symbol}/news`, `GET /api/stocks/{symbol}/orderbook`, `GET /api/stocks/{symbol}/investors`
- overview/quotes/news는 **캐시 키 고정**: `overview`, `quotes:us`, `quotes:kr`, `news:feed` — `cache.get_or_fetch(key, L2_TTL, fetcher)`로 조회(스케줄러가 선제 갱신하므로 보통 L1 히트). 종목 계열 키: `detail:{symbol}`, `chart:{symbol}:{period}`, `news:{symbol}`
- orderbook/investors 응답에 `"simulated": true` 포함. 알 수 없는 symbol(유니버스 밖)은 404
- envelope 적용: 모든 응답 `{"asOf", "marketOpen", "data"}` (+시뮬레이션은 data에 simulated)

- [ ] **Step 1: conftest 작성** — 서비스 전부 monkeypatch(고정 데이터), FakeL2로 TieredCache 구성, `TestClient(create_app())` 픽스처
- [ ] **Step 2: 실패 테스트 작성** — overview 200+envelope 필드, quotes?market=xx 422, 미지 심볼 404, chart period 검증 422, orderbook `simulated==True`, health에 `sources.yahoo` 존재. (테스트 코드는 conftest 픽스처 기반 5~7 assert)
- [ ] **Step 3: FAIL 확인** → **Step 4: 구현** — `create_app()` 팩토리(`main.py`는 B12), 라우터 등록, `asyncio.to_thread`로 동기 서비스 호출 감쌈 → **Step 5: PASS**
- [ ] **Step 6: Commit** — `git commit -m "feat(backend): market/stocks/health API routes"`

---

### Task B11: AI API + rate limit

**Files:**
- Create: `backend/app/api/ai.py`, `backend/app/api/ratelimit.py`
- Test: `backend/tests/test_api_ai.py`

**Interfaces:**
- Consumes: B9 `analyze_stock`/`analyze_article`, B7 `fetch_article_content`, B3 캐시
- Produces: `ratelimit.SlidingWindowLimiter(limit: int, window_sec: int)` — `.allow(ip: str) -> bool` (deque 기반); `POST /api/ai/stocks/{symbol}` (body 없음), `POST /api/ai/articles` (body `{"url": str, "title": str, "language": "ko"|"en"}`)
- 동작: IP = `X-Forwarded-For` 첫 항목 (없으면 client.host) → limiter 초과 429 `{"detail":"rate_limited","retryAfter":60}` → 캐시 키 `ai:stock:{symbol}` / `ai:article:{sha1(url)[:16]}` TTL `AI_TTL` 히트 시 즉시 반환 → miss 시 `asyncio.Semaphore(AI_GLOBAL_CONCURRENCY)` 안에서 Bedrock 호출 → `BedrockUnavailableError`는 503 `{"detail":"ai_unavailable"}`

- [ ] **Step 1: 실패 테스트 작성** — FakeBedrock으로: 첫 호출 200+텍스트, 같은 심볼 재호출은 Bedrock 미호출(카운터)로 200, 같은 IP 4번째 호출 429, `X-Forwarded-For: 1.2.3.4, 10.0.0.1` 헤더가 첫 IP 기준으로 제한되는지, Unavailable 시 503.
- [ ] **Step 2: FAIL 확인** → **Step 3: 구현** → **Step 4: PASS**
- [ ] **Step 5: Commit** — `git commit -m "feat(backend): AI endpoints with rate limit + result cache"`

---

### Task B12: 스케줄러 + main.py + 정적 서빙 + 로컬 스모크

**Files:**
- Create: `backend/app/core/scheduler.py`, `backend/app/main.py`
- Test: `backend/tests/test_scheduler.py`
- Create: `backend/README.md` (로컬 실행 방법 5줄)

**Interfaces:**
- Consumes: 전 태스크
- Produces: `scheduler.refresh_market(state) -> None`(overview/quotes:us/quotes:kr 갱신+`source_status["yahoo"]` 갱신), `scheduler.refresh_news(state)`, `scheduler.run_loops(state, stop: asyncio.Event)` — 두 루프: market은 `refresh_interval(now)`초 대기, news는 120초(휴장 시 600). `main.create_app()` → lifespan에서 DynamoCache 초기화(`CACHE_TABLE` 접근 실패 시 L2 없이 기동 + warning — 로컬 개발 배려), run_loops 기동/종료. `/` 및 SPA 라우트는 `StaticFiles(directory="static", html=True)` + 404 fallback to `index.html` (frontend 빌드 산출물, 없으면 skip)

- [ ] **Step 1: 실패 테스트 작성** — `refresh_market`을 mock 서비스로 1회 호출 → 캐시에 3개 키 존재 + `source_status["yahoo"] == "ok"`; 서비스 예외 발생 시 → 기존 캐시 유지 + `"degraded"` 마킹(예외 미전파).
- [ ] **Step 2: FAIL 확인** → **Step 3: 구현** → **Step 4: PASS + 전체 테스트** — `pytest -q` 전체 GREEN
- [ ] **Step 5: 로컬 스모크** — `.venv/bin/uvicorn app.main:app --port 8000 &` 후 `curl -s localhost:8000/api/health | python3 -m json.tool` → 200 확인, `curl "localhost:8000/api/market/overview"` → 실데이터(네트워크 가능 환경) 또는 degraded envelope 확인. 프로세스 종료.
- [ ] **Step 6: Commit** — `git commit -m "feat(backend): scheduler loops, app factory, static serving"`

---

## Self-Review 결과 반영 사항

- B5 `fetch_market_caps`는 대시보드 테이블 시가총액용 — B12 `refresh_market`에서 quotes 갱신 후 백그라운드 1회/10분 주기로 호출해 Quote.market_cap 채움 (TUI 4-Wave의 Wave4 대응)
- KOSDAQ 접미사 목록은 B1 Step 2에 명시된 6종목만 `.KQ` (TUI KR_STOCKS 기준 검증 완료)
- 모든 태스크 독립 실행 가능: B5~B9는 B3/B4와 병렬 가능, B10은 B5~B8 완료 후
