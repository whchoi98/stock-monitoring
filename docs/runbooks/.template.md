# Runbook: Title

<a href="#english"><img src="https://img.shields.io/badge/lang-English-blue.svg" alt="English"></a>
<a href="#한국어"><img src="https://img.shields.io/badge/lang-한국어-red.svg" alt="한국어"></a>

<!--
File name: kebab-case verb phrase (e.g. deploy-production.md, rollback-production.md).
Commands must be copy-paste ready from the repository root unless stated otherwise.
Both language sections must carry identical content.
-->

---

<a id="english"></a>

# English

## Overview
<!-- Purpose of this runbook, in one or two sentences. -->

## When to Use
<!-- Circumstances that trigger this runbook (alarm fired, release approved, ...). -->

## Prerequisites
<!-- Required permissions, tools, and state. For this project typically:
     AWS credentials for ap-northeast-2, Docker running (image builds),
     infra/.venv installed (pinned CDK CLI). -->
- [ ] <prerequisite 1>
- [ ] <prerequisite 2>

## Procedure

### 1. <First Phase>
```bash
# copy-paste ready command
```
<!-- Expected output / what success looks like. -->

### 2. <Second Phase>
```bash
# copy-paste ready command
```

## Verification
- [ ] <check 1 — e.g. `curl -fsS https://d2wa9w1vbqlndl.cloudfront.net/api/health` returns 200>
- [ ] <check 2 — e.g. `bash scripts/smoke.sh <CloudFrontURL> <AlbDNS>` passes all 4 checks>

## Rollback
<!-- Recovery procedure if something goes wrong. State the point of no return, if any. -->

## Notes
- Last verified: YYYY-MM-DD
- <gotchas, timing (e.g. deploy takes ~4 min), related alarms>

---

<a id="한국어"></a>

# 한국어

## 개요
<!-- 이 런북의 목적을 한두 문장으로. -->

## 사용 시점
<!-- 이 런북을 따라야 하는 상황 (알람 발생, 릴리스 승인 등). -->

## 사전 요구 사항
<!-- 필요한 권한, 도구, 상태. 이 프로젝트에서는 보통:
     ap-northeast-2용 AWS 자격 증명, Docker 실행 중(이미지 빌드),
     infra/.venv 설치 완료(고정된 CDK CLI). -->
- [ ] <요구 사항 1>
- [ ] <요구 사항 2>

## 절차

### 1. <첫 번째 단계>
```bash
# 복사-붙여넣기 가능한 명령
```
<!-- 기대 출력 / 성공 판단 기준. -->

### 2. <두 번째 단계>
```bash
# 복사-붙여넣기 가능한 명령
```

## 검증
- [ ] <확인 1 — 예: `curl -fsS https://d2wa9w1vbqlndl.cloudfront.net/api/health`가 200 반환>
- [ ] <확인 2 — 예: `bash scripts/smoke.sh <CloudFrontURL> <AlbDNS>` 4개 검사 전부 통과>

## 롤백
<!-- 문제 발생 시 복구 절차. 되돌릴 수 없는 지점이 있다면 명시한다. -->

## 참고
- 최종 검증일: YYYY-MM-DD
- <주의점, 소요 시간(예: 배포 ~4분), 관련 알람>
